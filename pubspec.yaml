name: task_manager
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"   # Compatível com Dart 3.9.2 do Flutter 3.35

dependencies:
  flutter:
    sdk: flutter

  # ---- SUAS LIBS (no MESMO nível de 'flutter', não dentro dele) ----
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.1.3
  path: ^1.9.1
  uuid: ^4.5.1
  intl: ^0.20.2
  share_plus: ^7.2.0
  sqflite_common_ffi: ^2.3.6

  # shared_preferences (adicione se for usar persistência simples)
  # shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
